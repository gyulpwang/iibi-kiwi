<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0/EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="service.model.FaqMapper">

   <select id="allFaqList" parameterType="int" resultType="Faq">
       select distinct * from faq where faq_uptype=#{faq_uptype}
   </select>
   
</mapper>